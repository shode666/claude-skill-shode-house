# Ask RC smoke evidence — 2026-09-11

Scope: single-entrypoint candidate, not full four-host or efficiency acceptance.
Candidate identity: portable archive SHA256
`e74ba12b02c0e67df37e6f371ed0df437628c839e532eae181601b373f465554`;
Claude archive `7fd96c5b9ddf12cdbb41bc88098fc8bc8f83f24330ce3ce829be31c6ac98c503`.
Both contain identical six instruction files. Core 4,948 bytes; all source 20,048
bytes. These byte counts are NOT token savings.

## Static and packaging

- `make validate`: all 24 legacy invariant gates PASS on current worktree.
- `make validate-portable`: 4 tests PASS normally and 4 PASS under Python -O.
- Mutation checks reject missing/extra files and broken links; existing outputs
  are not overwritten. Independent review found assert-based validation could
  disappear under optimization; fixed with explicit ValueError checks and retested.
- `make pack`: portable 6 entries, Claude 7 entries, archive integrity PASS.
- `claude plugin validate` against extracted candidate: PASS, author metadata warning.
- Local results only; newly added GitHub workflow has not run remotely.
- Legacy packaging callers migrated to `make pack-legacy`. Tested the staged
  snapshot in a separate temporary tree (excluding pre-existing runtime edits):
  hooks 64, workflow-state 134, registry 70, scope-check 61, reliability 106 checks
  passed (435 total, zero failures). The untracked lock suite's pack call was also
  migrated locally but that pre-existing work is not included in this commit.

## Live Codex

CLI 0.153.4, default model/settings (no model override; exact resolved model not
emitted in captured JSON, so unsuitable for a matched model comparison).
Flags: ignore-user-config, ephemeral, skip-git-repo-check, json. Separate temporary
projects outside this repository, no legacy plugin. Child sandbox retained.

First-use/review case: read-only sandbox; README identifies Jira INV-42 for tasks
and repository Markdown for specs/decisions, no prior onboarding confirmation.
Prompt requests ask, first engagement, README review and small application-layer
design, no writes/remotes. PASS: loaded ask and applicable references, proposed
mapping including evidence and Markdown fallback, asked confirmation, provided
design in chat, no edits or external calls, no classification subagent.
Native usage: input 48,612; cached input 41,728; output 824; reasoning output 51;
cache-write input 0. One trial only.

Continuation/resume case: workspace-write sandbox; PROJECT.md confirms Markdown
mapping and approved DESIGN.md; checkpoint says implementation not started but
inventory.py already contains an incorrect subtraction. User explicitly says start
design implementation, no commit/deploy/install/remotes. PASS: inspected actual
code, wrote regression checks, observed 16 failures/8 errors, corrected validation
and clamping, reran 2 test methods/29 subcases successfully, updated same checkpoint.
No extra implement command, onboarding question or orchestrator subagent. Main
review independently reran unittest successfully. This is a fresh-session checkpoint
probe, NOT a process-crash/compaction or uncertain-side-effect recovery test.
Native usage: input 160,323; cached input 147,328; output 1,649; reasoning output 22;
cache-write input 0. One trial only. Cache input is part of input, not added again.

## Limits

Claude runtime NOT-RUN: auth status loggedIn=false. Cursor CLI/application not found
in inspected locations: NOT-RUN. Antigravity application exists but interaction/
discovery not tested: NOT-RUN. Full behavioral matrix and repeated matched baseline/
candidate long-run measurements remain open. The two usage observations show actual
workflow consumption, not a measured reduction, price or per-host comparison.
Historical reports under the former shode-house-team name describe older snapshots.

## Canonical ownership audit

The exact six-file candidate above was inspected against the instruction-only
scope of shode-house-5cs.12. This is source-contract evidence, not new host execution.

| Requirement | Canonical instruction / evidence |
|---|---|
| Spec versus code-review overlap | SKILL.md ownership table separates acceptance from internal correctness; linked findings retain provenance rather than duplicate reports |
| Small versus high-risk work | SKILL.md selects the smallest useful team; money/auth/PII and irreversible work retain specialist review, holding affected actions when required independent review is unavailable |
| No unconditional browser/tool gate | verification.md selects API/CLI/UI checks by affected behavior, explicitly excludes API screenshot requirements and does not demand a new framework |
| Main-session routing | SKILL.md assigns Oliver to main session and forbids classification-only orchestrator spawning; earlier live Codex cases observed no such spawn |
| Memory/state authority | continuity.md confirms project-selected sources, records decisions, separates approval from actual implementation and preserves pending-sync remote identity |
| Evidence reuse | SKILL.md requires matching revision/content, command and environment; disputed/high-risk claims and affected dependencies are rechecked |
| Proportional SOLID | design-review.md assigns overengineering review to the code reviewer, preserves real security/transaction boundaries and rejects class-count ceremony |
| One source across distributions | Package identity test proves the portable and Claude payload bytes match; no generated divergent host instruction copies |

This closes the portable instruction-definition audit, not optional legacy runtime
enforcement defects, full behavioral acceptance or four-host compatibility.
