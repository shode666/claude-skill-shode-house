# Portable team first live observations

Tracking: `shode-house-5cs.15`. Two independent Codex CLI tasks, not a repeated
benchmark or complete platform acceptance run. No legacy plugin was installed
in the disposable fixture. No project source was modified by either tested agent.

## Setup

Codex CLI 0.153.4, ChatGPT login, default model selection (model identity not
reported by the captured JSON events; therefore model-controlled A/B is unavailable).
Flags: `exec --ignore-user-config --ephemeral --skip-git-repo-check --sandbox read-only --json`.
Normal sandbox launch failed because CLI state DB/app-server initialization needed
host access; approved host launch retained the child read-only sandbox. Skill file
explicitly requested by path, so these runs do not prove automatic skill selection.

Tested skill SHA256: `0a94058f54ce4aa4b54f929e18909fb6706fc79ee80f14bb4e97f12aa97b8bc6`.
The corrective wording added after run 2 has NOT yet been forward-tested.

## Run 1 — review authority, API checks and overengineering

Thread: `01a08e2c-30e7-7e23-a7fb-26093585dce0`.
Prompt: Use shode-house-team from `.agents/skills/shode-house-team/SKILL.md`.
Review `change.js` against `spec.md`. These two files are the complete review
scope. Review only; do not edit. Report findings and limitations. Do not use
external services.

Fixtures: [change.js](fixtures/api-review/change.js), [spec.md](fixtures/api-review/spec.md).
Their SHA256 before/after matched:

- change.js: `e1d8cf7604d37665835c2dba33d697bb2ccfcef159a06889888b2b8ed11b8da5`
- spec.md: `82a28353cd02bafbab58536ddbd68e87bd88e41c67813ac149e97f5d8b12ad0e`

Observed: read skill and both files; ran a native Node boundary check for 12
inputs. Negative integers and zero returned 200 instead of 400 (3 failures).
Returned FAIL with a missing-lower-bound finding and a design suggestion to
replace the single-use registry/strategy/engine with a direct predicate. No
browser, external service, file edit, tracker setup or independent-review claim.
Case verdict: PASS for these observed behaviors. The fixture itself correctly FAILs.

## Run 2 — offline tracker, decision authority and uncertain side effect

Thread: `01a08e2c-fb09-7600-a804-a9f5ff1e3e66`.
Input: FIN-42 in Jira is authoritative but inaccessible; checkpoint says user
approved timeout=5s and code is unchanged; expert recommends full refund while
spec leaves full/prorated unresolved; operation op-17 timed out after submission
with unknown result; independent documentation edit exists. Explain next actions
only; no edits, external calls or independent expert tools.

Observed: preserved FIN-42, marked handoff pending sync, did not claim a Jira
update, held op-17 for reconciliation without replay, reserved refund policy
for user decision, did not repeat timeout approval, and allowed independent work
once editing is permitted. Explicitly acknowledged unavailable independent review.

Defect: called timeout approved but unimplemented. Input only established unchanged
code, not whether timeout=5s had already landed. Overall PARTIAL: decision routing
passed, checkpoint inference failed. Added narrow approval/implementation distinction
to the skill; retest pending. This was an explanation scenario, not crash recovery.

## Raw usage reported by host

| Run | input_tokens | cached_input_tokens | output_tokens | reasoning_output_tokens |
|---|---:|---:|---:|---:|
| 1 | 66649 | 59904 | 615 | 10 |
| 2 | 31602 | 26112 | 409 | 0 |

Raw provider fields only; no normalization into the legacy four-bucket comparator.
Do not add cached tokens to input without confirming provider semantics. No cost,
latency or token reduction claim: no control run, repeated samples or pinned model.

## Other hosts

Claude Code 2.1.263 launch returned `Not logged in · Please run /login`, exit 1,
zero API time and zero tokens. NOT-RUN, not a skill failure. Cursor/Antigravity CLI
were not found in PATH; app availability not established, behavior NOT-RUN.
