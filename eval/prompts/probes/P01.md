# P01 — routing probe: dev-gate vs diagnose/incident

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: `eval/fixtures/*.yaml` case `trig-tdd` (utterance verbatim)
- expected: see `eval/scenarios/golden.json` id `P01` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
เริ่มเขียน code แบบ test first red-green-refactor
```
