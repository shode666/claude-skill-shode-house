# P02 — routing probe: diagnose vs incident

- kind: `probe` (`--max-turns 3`; scorer asserts FIRST skill / FIRST spawn only)
- source: `eval/fixtures/*.yaml` case `trig-bug` (utterance verbatim)
- expected: see `eval/scenarios/golden.json` id `P02` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
ระบบพัง ไม่ทำงาน ช่วย debug หา root cause
```
