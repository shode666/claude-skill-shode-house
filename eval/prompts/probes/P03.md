# P03 — routing probe: incident vs diagnose

- kind: `probe` (`--max-turns 3`; scorer asserts FIRST skill / FIRST spawn only)
- source: `eval/fixtures/*.yaml` case `trig-prod-outage` (utterance verbatim)
- expected: see `eval/scenarios/golden.json` id `P03` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
service down ใน prod, P1, ต้อง rollback เดี๋ยวนี้
```
