# P04 — routing probe: data-migration (positive)

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: new in 3.17 (boundary case)
- expected: see `eval/scenarios/golden.json` id `P04` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
ต้องเพิ่ม column `currency` แบบ NOT NULL ให้ตาราง orders ที่มี 40 ล้านแถวบน production โดยห้ามมี downtime — ช่วยวางแผนให้หน่อย
```
