# P05 — routing probe: api-contract (positive)

- kind: `probe` (`--max-turns 3`; scorer asserts FIRST skill / FIRST spawn only)
- source: new in 3.17 (boundary case)
- expected: see `eval/scenarios/golden.json` id `P05` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
จะเปลี่ยน field `amount` ใน response ของ GET /v1/refunds จาก number เป็น string — มี mobile app กับ partner 3 รายเรียกอยู่ ต้องทำยังไงไม่ให้ฝั่งเขาพัง
```
