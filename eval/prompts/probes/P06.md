# P06 — routing probe: secure trigger (positive)

- kind: `probe` (`--max-turns 3`; scorer asserts FIRST skill / FIRST spawn only)
- source: `eval/fixtures/*.yaml` case `trig-threat-model` (utterance verbatim)
- expected: see `eval/scenarios/golden.json` id `P06` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
ช่วยทำ STRIDE threat model ให้ feature payment
```
