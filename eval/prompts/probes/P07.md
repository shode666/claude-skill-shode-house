# P07 — routing probe: ui-test vs web-q

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: `eval/fixtures/*.yaml` case `trig-e2e` (utterance verbatim)
- expected: see `eval/scenarios/golden.json` id `P07` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
เขียน Playwright E2E + visual regression + axe a11y
```
