# P08 — routing probe: decompose vs drain

- kind: `probe` (`--max-turns 3`; scorer asserts FIRST skill / FIRST spawn only)
- source: new in 3.17 (boundary case)
- expected: see `eval/scenarios/golden.json` id `P08` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
spec refund ใน outputs/SPEC-bd-101.md sign-off แล้ว ช่วยแตกเป็นงานย่อยที่ verify ได้ พร้อม owner, acceptance criteria และ dependency
```
