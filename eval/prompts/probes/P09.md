# P09 — routing probe: drain vs decompose

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: new in 3.17 (boundary case)
- expected: see `eval/scenarios/golden.json` id `P09` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
มี issue ที่ ready อยู่ 6 ใบ แต่ละใบ independent กันและ scope ชัดแล้ว — จัดการส่งมอบให้ครบแบบขนาน มี review อิสระทุกใบก่อนปิด
```
