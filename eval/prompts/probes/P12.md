# P12 — routing probe: web-q vs ui-test

- kind: `probe` (`--max-turns 3`; scorer asserts FIRST skill / FIRST spawn only)
- source: `eval/fixtures/*.yaml` case `trig-cwv` (utterance verbatim)
- expected: see `eval/scenarios/golden.json` id `P12` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
audit Core Web Vitals LCP INP CLS + Lighthouse
```
