# P13 — routing probe: automate-test vs ui-test/dev-gate

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: new in 3.17 (boundary case)
- expected: see `eval/scenarios/golden.json` id `P13` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
project ใหม่ยังไม่มี test เลย ช่วยวางโครง test pyramid + coverage gate ใน CI ให้หน่อย
```
