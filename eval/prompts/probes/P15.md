# P15 — routing probe: domain expert: fintech ledger rule

- kind: `probe` (`--max-turns 3`; scorer asserts FIRST skill / FIRST spawn only)
- source: `eval/fixtures/*.yaml` case `route-ledger` (utterance verbatim)
- expected: see `eval/scenarios/golden.json` id `P15` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
ออกแบบ double-entry ledger สำหรับ e-wallet รองรับ PromptPay
```
