# P16 — routing probe: NEGATIVE plain SQL query is not data-migration

- kind: `probe` (`--max-turns 3`; scorer asserts FIRST skill / FIRST spawn only)
- source: new in 3.17 (boundary case)
- expected: see `eval/scenarios/golden.json` id `P16` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
เขียน SQL query หายอดขายรวมต่อเดือนจากตาราง orders ย้อนหลัง 6 เดือน (SELECT อย่างเดียว ไม่แก้ schema ไม่แก้ข้อมูล)
```
