# P17 — routing probe: NEGATIVE private function is not api-contract

- kind: `probe` (`--max-turns 3`; scorer asserts FIRST skill / FIRST spawn only)
- source: new in 3.17 (boundary case)
- expected: see `eval/scenarios/golden.json` id `P17` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
ใน src/report.py แยก loop ของ export_sales ออกเป็น private function `_filter_rows` — ไม่มีใครนอกไฟล์นี้เรียก พฤติกรรมเดิมทุกอย่าง
```
