# P18 — routing probe: NEGATIVE local refactor is not a secure trigger

- kind: `probe` (`--max-turns 3`; scorer asserts FIRST skill / FIRST spawn only)
- source: new in 3.17 (boundary case)
- expected: see `eval/scenarios/golden.json` id `P18` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
refactor src/notification.py: แยก retry loop ออกเป็นฟังก์ชันย่อย พฤติกรรมเดิมทุกอย่าง ไม่แตะ interface
```
