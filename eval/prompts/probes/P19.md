# P19 — routing probe: NEGATIVE backend-only test is not ui-test

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: new in 3.17 (boundary case)
- expected: see `eval/scenarios/golden.json` id `P19` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
เขียน unit test ให้ src/validators.py require_email() ครอบกรณีไม่มี email กับ email ถูกต้อง — backend ล้วน ไม่มีหน้าจอ
```
