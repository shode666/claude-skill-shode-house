# P20 — routing probe: NEGATIVE variable merely named payment is not a fintech trigger

- kind: `probe` (`--max-turns 3`; scorer asserts FIRST skill / FIRST spawn only)
- source: new in 3.17 (boundary case)
- expected: see `eval/scenarios/golden.json` id `P20` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
ใน src/report.py เปลี่ยนชื่อตัวแปร loop `r` เป็น `payment` ให้อ่านง่ายขึ้น ไม่เปลี่ยน logic
```
