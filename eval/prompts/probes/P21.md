# P21 — routing probe: legacy: caveman

- kind: `probe` (`--max-turns 3`; scorer asserts FIRST skill / FIRST spawn only)
- source: `eval/fixtures/*.yaml` case `trig-compress` (utterance verbatim)
- expected: see `eval/scenarios/golden.json` id `P21` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
พูดสั้นๆ caveman mode ประหยัด token
```
