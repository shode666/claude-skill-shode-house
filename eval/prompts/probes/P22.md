# P22 — routing probe: legacy NEGATIVE smalltalk

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: `eval/fixtures/*.yaml` case `trig-negative-smalltalk` (utterance verbatim)
- expected: see `eval/scenarios/golden.json` id `P22` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
สวัสดีครับ วันนี้อากาศดี
```
