# P23 — routing probe: legacy routing: trading-expert

- kind: `probe` (`--max-turns 3`; scorer asserts FIRST skill / FIRST spawn only)
- source: `eval/fixtures/*.yaml` case `route-matching-engine` (utterance verbatim)
- expected: see `eval/scenarios/golden.json` id `P23` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
ออกแบบ matching engine crypto exchange รับ limit/market/stop
```
