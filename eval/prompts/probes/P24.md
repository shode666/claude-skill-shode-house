# P24 — routing probe: legacy routing: insurance-expert

- kind: `probe` (`--max-turns 3`; scorer asserts FIRST skill / FIRST spawn only)
- source: `eval/fixtures/*.yaml` case `route-policy-admin` (utterance verbatim)
- expected: see `eval/scenarios/golden.json` id `P24` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
ออกแบบ policy admin รถยนต์ รองรับ endorsement + renewal
```
