# P25 — routing probe: legacy routing: ux-ui-designer

- kind: `probe` (`--max-turns 3`; scorer asserts FIRST skill / FIRST spawn only)
- source: `eval/fixtures/*.yaml` case `route-checkout` (utterance verbatim)
- expected: see `eval/scenarios/golden.json` id `P25` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
ออกแบบ flow checkout ใหม่ให้กดง่ายขึ้น
```
