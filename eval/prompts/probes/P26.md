# P26 — routing probe: legacy routing: devops-engineer

- kind: `probe` (`--max-turns 3`; scorer asserts FIRST skill / FIRST spawn only)
- source: `eval/fixtures/*.yaml` case `route-docker-ci` (utterance verbatim)
- expected: see `eval/scenarios/golden.json` id `P26` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
setup FastAPI ใหม่พร้อม Docker + CI
```
