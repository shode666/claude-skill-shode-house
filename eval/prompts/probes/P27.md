# P27 — routing probe: legacy routing: code-reviewer

- kind: `probe` (`--max-turns 3`; scorer asserts FIRST skill / FIRST spawn only)
- source: `eval/fixtures/*.yaml` case `route-unit-test` (utterance verbatim)
- expected: see `eval/scenarios/golden.json` id `P27` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
review payment service + เขียน unit test ให้
```
