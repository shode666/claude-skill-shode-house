# P28 — routing probe: diagnose: works-on-my-machine flaky behaviour, not yet released

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: paraphrase-v1 (Bella, keyword-free) — bd `shode-house-8ss.3`; written without reading skill descriptions / trigger fixtures
- expected: see `eval/scenarios/golden.json` id `P28` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
ตอนรันในเครื่องตัวเอง src/notification.py บางรอบส่งเมลออกไปสามฉบับซ้ำกัน บางรอบไม่ออกเลยสักฉบับ ทั้งที่ข้อมูลเข้าเหมือนเดิม ยังไม่ได้ขึ้นให้ลูกค้าใช้นะ แค่เครื่องผมกับเครื่องน้องในทีม ไล่ดูมาครึ่งวันแล้วยังไม่รู้ว่าเพราะอะไร ช่วยดูหน่อย
```
