# P29 — routing probe: incident: customers affected right now after a release

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: paraphrase-v1 (Bella, keyword-free) — bd `shode-house-8ss.3`; written without reading skill descriptions / trigger fixtures
- expected: see `eval/scenarios/golden.json` id `P29` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
ลูกค้าโทรเข้ามาเป็นสิบสายแล้วว่ากดขอคืนเงินแล้วหน้าจอหมุนค้าง เงินก็ไม่เข้า เริ่มเป็นราวบ่ายสองหลังปล่อยของรอบล่าสุด ตอนนี้ก็ยังเป็นอยู่และสายยังเข้าเรื่อยๆ ต้องทำอะไรก่อน
```
