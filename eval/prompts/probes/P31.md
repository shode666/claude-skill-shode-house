# P31 — routing probe: data-migration: change stored shape of 40M rows while live

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: paraphrase-v1 (Bella, keyword-free) — bd `shode-house-8ss.3`; written without reading skill descriptions / trigger fixtures
- expected: see `eval/scenarios/golden.json` id `P31` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
ตารางเก็บรายการเดินบัญชีของเราตอนนี้เก็บยอดเงินเป็นเลขทศนิยมลอย มีอยู่ราว 40 ล้านแถว อยากเปลี่ยนไปเก็บเป็นจำนวนเต็มหน่วยสตางค์แทน โดยที่ลูกค้ายังใช้งานได้ตลอดระหว่างเปลี่ยน และถ้าพลาดต้องถอยกลับได้ ควรวางขั้นตอนยังไง
```
