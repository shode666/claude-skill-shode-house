# P32 — routing probe: api-contract: rename+require a field other teams/partners consume

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: paraphrase-v1 (Bella, keyword-free) — bd `shode-house-8ss.3`; written without reading skill descriptions / trigger fixtures
- expected: see `eval/scenarios/golden.json` id `P32` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
ตอนนี้ทีมแอปมือถือกับพาร์ตเนอร์อีกสองเจ้าเรียกตัวขอคืนเงินของเราอยู่ (ตาม outputs/SPEC-bd-101.md) เราอยากเปลี่ยนชื่อ reason เป็น reason_code แล้วบังคับว่าต้องส่งมาทุกครั้ง จะทำยังไงไม่ให้ของฝั่งเขาพังวันที่เราปล่อย
```
