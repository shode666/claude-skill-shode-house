# P33 — routing probe: secure: internal-only capability opened to outside parties

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: paraphrase-v1 (Bella, keyword-free) — bd `shode-house-8ss.3`; written without reading skill descriptions / trigger fixtures
- expected: see `eval/scenarios/golden.json` id `P33` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
เดิมตัวขอคืนเงินใช้กันแค่พนักงานข้างในออฟฟิศ รอบหน้าจะเปิดให้ร้านค้าข้างนอกยิงเข้ามาขอคืนเงินแทนลูกค้าได้เองผ่านอินเทอร์เน็ต ก่อนเปิดเราต้องคิดอะไรบ้าง เช่น รู้ได้ไงว่าเป็นร้านนั้นจริง ร้านหนึ่งจะไปแตะรายการของอีกร้านได้ไหม
```
