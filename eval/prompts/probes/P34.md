# P34 — routing probe: ui-test: screen changed, want repeatable check of behaviour

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: paraphrase-v1 (Bella, keyword-free) — bd `shode-house-8ss.3`; written without reading skill descriptions / trigger fixtures
- expected: see `eval/scenarios/golden.json` id `P34` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
หน้าประวัติการคืนเงิน (outputs/bd-102) เพิ่งแก้ให้แถวยุบเป็นการ์ดตอนจอแคบ อยากมั่นใจว่ากดไล่ด้วยคีย์บอร์ดยังไปตามลำดับเดิม และตอนไม่มีรายการยังมีปุ่มพาไปต่อ ไม่อยากให้คนมานั่งกดดูเองทุกครั้งที่แก้หน้านี้ ช่วยจัดการให้หน่อย
```
