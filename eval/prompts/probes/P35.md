# P35 — routing probe: slo: how much unreliability is acceptable, and what happens when exceeded

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: paraphrase-v1 (Bella, keyword-free) — bd `shode-house-8ss.3`; written without reading skill descriptions / trigger fixtures
- expected: see `eval/scenarios/golden.json` id `P35` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
ฝั่งธุรกิจถามมาว่าตัวขอคืนเงินใช้ไม่ได้เดือนละกี่นาทีถึงจะยังถือว่ารับได้ แล้วถ้าเดือนไหนเกินเราควรหยุดปล่อยของใหม่ก่อนไหม ตอนนี้ยังไม่เคยตกลงตัวเลขอะไรกันเลย ช่วยตั้งให้หน่อย
```
