# P39 — routing probe: NEGATIVE 'test' meaning a user comprehension test of wording is not test automation

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: paraphrase-v1 (Bella, keyword-free) — bd `shode-house-8ss.3`; written without reading skill descriptions / trigger fixtures
- expected: see `eval/scenarios/golden.json` id `P39` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
ข้อความ "send failed" ใน src/notification.py คนอ่านแล้วงง ช่วยคิดข้อความใหม่ให้สัก 3 แบบ พรุ่งนี้จะเอาไป test กับผู้ใช้ 5 คนว่าแบบไหนเข้าใจง่ายสุด ยังไม่ต้องแก้ไฟล์
```
