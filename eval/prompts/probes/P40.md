# P40 — routing probe: discovery/priority decision — is this worth building (Patrick)

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: authored-blind-3.17 (unmeasured owner coverage) — written from the single-owner capability matrix, without reading any proposed dispatch-floor wording
- expected: see `eval/scenarios/golden.json` id `P40` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
มีผู้ใช้ขอ feature ให้ตั้งเงื่อนไขการแจ้งเตือนเองได้มา 6 รายแล้ว ในทีมเถียงกันว่าควรทำหรือปล่อยไว้ก่อน ช่วยประเมินให้หน่อยว่ามันคุ้มพอจะลงมือทำไหม ถ้าคุ้มควรอยู่ลำดับไหนเทียบกับของที่ค้างอยู่ตอนนี้ — ยังไม่ต้องออกแบบหรือเขียน code
```
