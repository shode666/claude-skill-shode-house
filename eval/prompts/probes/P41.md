# P41 — routing probe: cross-team consistency + tech radar + large refactor strategy (Stan)

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: authored-blind-3.17 (unmeasured owner coverage) — written from the single-owner capability matrix, without reading any proposed dispatch-floor wording; rewritten after validation 53 to remove the tech-stack reading that belongs to Sara
- expected: see `eval/scenarios/golden.json` id `P41` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
All three backend teams are already on the same agreed stack, but the same retry-and-error-handling pattern is now written four different ways across their services and every review re-litigates it. Look across the teams, say which one we should standardise on and put on the tech radar, and lay out how a refactor that wide gets rolled through the existing services without freezing feature work for a quarter.
```
