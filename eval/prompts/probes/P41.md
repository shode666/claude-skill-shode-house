# P41 — routing probe: convention standard + tech radar + refactor strategy (Stan)

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: authored-blind-3.17 (unmeasured owner coverage) — rewritten after validation 53 (tech-stack reading belonged to Sara) and again after the BASE adjudication 54 (the premise was false in the fixture); every fact it now states is verifiable in `src/` (overstatement 'every module' corrected after the 53 repair re-check)
- expected: see `eval/scenarios/golden.json` id `P41` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
The modules in src/ signal failure three different ways: validators.py raises ValueError with an error-code prefix, ledger.py raises a bare ValueError, and notification.py swallows the exception, prints it and returns False (report.py has no failure path at all). Decide which of the three we adopt as the standard for everyone who builds on this code, put that on the tech radar, and lay out how the refactor rolls through the existing modules without freezing feature work.
```
