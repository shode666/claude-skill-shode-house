# P42 — routing probe: ERP business-rule decision — period close accrual (Elena)

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: authored-blind-3.17 (unmeasured owner coverage) — written from the single-owner capability matrix, without reading any proposed dispatch-floor wording
- expected: see `eval/scenarios/golden.json` id `P42` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
ตอนปิดงวดบัญชี ของที่รับเข้าคลังแล้วแต่ใบแจ้งหนี้ผู้ขายยังไม่มา ควรลงบัญชีเป็นอะไรในงวดนั้น แล้วพองวดถัดไปใบแจ้งหนี้มาจริงและราคาไม่ตรงกับตอนรับของ ต้องกลับรายการและปรับต้นทุนยังไงให้ยอดคงคลังกับบัญชีแยกประเภทตรงกัน ขอกฎที่ใช้ตัดสินให้ชัดก่อน แล้วค่อยว่ากันเรื่องระบบ
```
