# P43 — routing probe: SAP decision — custom field vs standard extension before conversion (Sam)

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: authored-blind-3.17 (unmeasured owner coverage) — written from the single-owner capability matrix, without reading any proposed dispatch-floor wording
- expected: see `eval/scenarios/golden.json` id `P43` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
We are still on ECC 6.0 and the conversion to S/4HANA is planned for next year. Finance wants an extra attribute on the customer master that has to drive a different pricing condition in sales orders. Should that attribute be an append on the standard master table, or a separate Z table read by a condition routine? I need the rule that decides it — which option survives the conversion, and what happens to the existing condition records either way.
```
