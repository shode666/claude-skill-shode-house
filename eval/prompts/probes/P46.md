# P46 — routing probe: NEGATIVE a one-line fix stays a one-line fix even when pitched as top business priority

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: authored-blind-3.17 (over-dispatch guard) — re-pointed after the BASE adjudication 54 at real fixture code (`src/validators.py:5`)
- expected: see `eval/scenarios/golden.json` id `P46` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
อันนี้ด่วนสุดของสัปดาห์นี้ ลูกค้าองค์กรรออยู่ กระทบรายได้แน่ๆ — ข้อความ error ใน src/validators.py บรรทัดที่ raise ValueError สะกดผิดสองคำ "vaild emial" ต้องเป็น "valid email" แก้บรรทัดนั้นบรรทัดเดียวพอ ไม่ต้องแตะอย่างอื่น
```
