# P47 — routing probe: NEGATIVE a single-file rename is not a cross-team consistency programme

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: authored-blind-3.17 (over-dispatch guard) — re-pointed after the BASE adjudication 54 at a symbol that exists (`src/notification.py:3`, used only on line 5 of the same file)
- expected: see `eval/scenarios/golden.json` id `P47` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
ทีมตกลงกันแล้วว่าค่าคงที่ที่เป็นจำนวนครั้งสูงสุดจะขึ้นต้นด้วย MAX_ ให้เหมือนกันทั้งโปรเจกต์ ตอนนี้เหลือ RETRY ใน src/notification.py ที่ยังไม่ตรง ช่วยเปลี่ยนเป็น MAX_RETRIES ในไฟล์นั้น ไม่มีไฟล์อื่นเรียกใช้ พฤติกรรมเหมือนเดิมทุกอย่าง
```
