# P47 — routing probe: NEGATIVE a single-file rename is not a cross-team consistency programme

- kind: `probe` (`--max-turns 6`; scorer: `route_any` = listed skill loaded or listed agent dispatched by the main session, anywhere in the run)
- source: authored-blind-3.17 (over-dispatch guard) — written from the single-owner capability matrix, without reading any proposed dispatch-floor wording
- expected: see `eval/scenarios/golden.json` id `P47` (single source; do not duplicate here)

## Prompt (ใช้ verbatim ทุกรอบ ห้ามแก้หลังเก็บ baseline)

```
ทีมตกลงกันแล้วว่าทั้งโปรเจกต์จะเรียกว่า customer ไม่ใช่ client เพื่อให้เรียกเหมือนกันทั้งระบบ ตอนนี้เหลือ src/mailer.py ไฟล์เดียวที่ยังใช้ client_name อยู่ ช่วยเปลี่ยนชื่อในไฟล์นั้นให้ตรงกัน ไม่มีที่อื่นเรียกใช้ พฤติกรรมเหมือนเดิมทุกอย่าง
```
