# 3.16.0-rc.10 — Ask Oliver (host configuration)

Candidate, not a published release. Durable release status: Beads shode-house-5cs.

RC10 adds Codex UI metadata and an explicit Claude manifest template. Both packages
carry the same six RC9 instruction files byte-for-byte. Codex adds one YAML file;
Claude adds its plugin JSON with version/description from release/3.16.json. No
runtime scripts, user settings overrides or custom Oliver subagent are added.
New metadata is statically validated, not a new live-host/token acceptance result.

Qualification completed: scoped Codex behavior and native recovery pass; combined
token gates FAIL. RC9 median total volume falls 18.23% for review and 4.52% for
resume, but uncached-input-plus-output rises 49.09% and 10.20% respectively.
Remain a release candidate: no stable promotion or token-gate waiver has been approved.
See [RC9 benchmark](../eval/portable-team/BENCHMARK-RC9-2026-09-11.md),
[current behavior/recovery](../eval/portable-team/BEHAVIOR-RC9-2026-09-11.md),
[RC7 benchmark](../eval/portable-team/BENCHMARK-RC7-2026-09-11.md) and
[RC8 benchmark](../eval/portable-team/BENCHMARK-RC8-2026-09-11.md).

RC9 adds a bounded low-risk route: routine fixes/reviews with clear acceptance and
existing tests use core checks directly, retaining continuity for resume. Non-routine
design/testing and high-risk work still load their guidance. Matching recorded red
evidence is reusable; do not repeat tests purely for ceremony. RC8 review quality
passes but total-volume median still fails; wording reduction alone was insufficient.
Before any RC9 trials: repeat the same two-scenario protocol, controls, prompts,
three independent trials per arm and unchanged +3% median/+5% p90 gates. Freeze
source and inputs, retain all RC7/RC8 results separately. No sample pooling.

RC8 narrows verification guidance: inspect existing tests before creating a
redundant probe; retain public-boundary evidence, independent expected values,
regression red/green checks, unchanged acceptance gates and authorization limits.
RC7 review quality passes but effective-token p90 exceeds its unchanged +5% gate.
RC8 will use a new frozen batch of the same review and two-session scenarios,
three independent trials per arm, with unchanged controls/prompts/settings/limits.
Do not pool samples with RC7. Its complete results remain separate evidence.

The user narrowed stable support to Codex. Claude Code, Cursor and Antigravity
remain experimental/unverified and are excluded from this release's support gate,
not marked PASS. RC7 removes repeated entrypoint/continuity prose and redundant
inventory guidance; [qualification protocol](../eval/portable-team/CODEX-STABLE-PROTOCOL.md)
retains quality, recovery and both existing token-regression thresholds. Completed
results above supersede the pre-trial plans, not their unchanged gates.

RC5's selective-history entrypoint correction failed its targeted probe: both
candidate sessions still read history without a demonstrated need. RC6 makes the
read dependency explicit for reassessment as well as resume. Targeted retest passes
2/2 no-discrepancy sessions and one history-needed positive control; see
[RC6 evidence](../eval/portable-team/HISTORY-RC6-2026-09-11.md). Token metrics remain
mixed and do not establish overall savings or full efficiency acceptance.
See [results and retest protocol](../eval/portable-team/HISTORY-RC5-2026-09-11.md).

RC4 narrows checkpoint evidence to command/result and source/environment, avoids
restating specs/coverage, and moves costly resolved history behind a verified pointer.
Its bounded growth probe preserves safety and reduces current-record size, but
still reads history unnecessarily and has mixed token metrics; see
[RC4 results](../eval/portable-team/GROWTH-RC4-2026-09-11.md).
RC3's growth probe preserved safety but
still expanded unchanged records and used 6.2% more total token volume in its one
chain than baseline; see [RC3 growth results](../eval/portable-team/GROWTH-RC3-2026-09-11.md).

RC3 added current-checkpoint maintenance: update current facts, link retained history
on demand, and preserve unresolved decisions, approvals and UNKNOWN operations.
Do not discard the only copy of history or rewrite immutable tracker records.
No new runtime files or scripts. The recovery results below describe RC2, not RC6. See the
[checkpoint evaluation protocol](../eval/portable-team/CHECKPOINT-RC3-PROTOCOL.md).

RC2 narrows continuity loading for already-confirmed standalone review/explanation
and discourages redundant file inventories. RC1's six-trial Codex review benchmark
passed finding quality but failed token-volume regression. RC2's six-trial review
batch confirms the loading correction and finding quality; total-volume median
fell 15.4% against its fresh baseline, but the combined efficiency gate still fails
on uncached-input-plus-output (+3.7%). Small-sample variability is substantial.
See [RC2 evidence](../eval/portable-team/BENCHMARK-RC2-2026-09-11.md).
Three Codex read-only synthetic first-use/checkpoint probes pass; see
[continuity evidence](../eval/portable-team/CONTINUITY-RC2-2026-09-11.md).
A two-process Codex test also passes actual checkpoint write and fresh-session
continuation after a planned clean exit; see
[resume evidence](../eval/portable-team/RESUME-RC2-2026-09-11.md).
A forced-stop probe also passes recovery after a completed code edit left the
checkpoint stale; see [interruption evidence](../eval/portable-team/INTERRUPT-RC2-2026-09-11.md).
Partial-write/compaction recovery and full host acceptance remain unverified for
RC2; no stable/savings claim. The interrupted run has incomplete token accounting.

One entrypoint, `ask`, with Oliver in the main session. Consultation, design,
implementation and review are outcomes; an explicit request can continue the
identified design without another command. Review/design approval does not grant
implementation, commit or deployment authority.

First use confirms project-selected sources of truth. Markdown is the fallback
for tasks, specs, decisions, checkpoints and evidence summaries; remote IDs and
pending synchronization remain explicit. Resume checks actual artifacts and stale
evidence before proceeding; uncertain external effects must not be blindly replayed.

Six Markdown files replace the released legacy workflow surface. References load
by applicability; no runtime scripts, hooks, tracker, interpreter or mandatory team
fan-out. Project tools and host capabilities still constrain execution. This is
instructional coordination, not enforced locks or an autonomous scheduler.

Build with `make pack`; validate with `make validate-portable`. The portable ZIP
and Claude plugin share exact instruction bytes. Default packaging excludes legacy
commands/agents/hooks/output styles. Existing root legacy manifests remain for
migration; do not install this repository root as the new release. Follow
[installation guidance](../docs/portable-team-install.md).

Compatibility and token reduction are not proven by smaller source files or
archive validation. Matched repeated Codex whole-workflow efficiency/recovery and
behavioral acceptance remain release gates. Do not label other hosts verified.
